@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    <li>
        @Html.ActionLink(" " + User.Identity.GetUserName() + "", "", "", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <h style="opacity:0%"> __ </h>

    <li><a href="javascript:document.getElementById('logoutForm').submit()">تسجيل الخروج</a></li>
</ul>
    }
}
else
{
<ul class="nav navbar-nav navbar-right">

    @*<li>@Html.ActionLink("     إنشاء حساب     ", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", style = "color:auto;font-size:bold;font-size:20px " })</li>*@
    <h style="opacity:0%"> __ </h>
    <li>@Html.ActionLink("   تسجيل دخول   ", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", style = "color:auto;font-size:bold;font-size:20px " })</li>

</ul>
}
